/**
 * #평가
 * 코드가 계산되어 값을 만드는 것
 */

/**
 * #일급
 * 변수에 담을 수 있다
 * 함수의 인자로 사용될 수 있다
 * 함수의 결과로 사용될 수 있다
 */

const a = 10;
const add10 = a => a + 10;
const result = add10(a);
console.log(result);

/**
 * 일급 함수
 * 함수를 값으로 다룰 수 있다
 * 조합성과 추상화의 도구
 */

